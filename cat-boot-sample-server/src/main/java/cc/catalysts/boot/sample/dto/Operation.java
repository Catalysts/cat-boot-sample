package cc.catalysts.boot.sample.dto;

/**
 * Sample enum for cat-boot-enum. The enum name, values names and translations
 * are exposed over a JSON API.
 *
 * @author Joel Colledge
 */
public enum Operation {
    Addition,
    Multiplication,
    Division
}
